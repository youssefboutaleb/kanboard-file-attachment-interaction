vendor/
.phpunit.result.cache
.DS_Store
*.log

# Build artifacts — release archives are published as GitHub Release assets
# (see .github/workflows/release.yml), never committed to the repository.
dist/
